import ComingSoon from "@/components/savameta/ComingSoon";

export default function TriggersPage() {
  return (
    <ComingSoon
      title="Triggers"
      description="Detect returning users / first value achievers / first resolution achievers. Export CSV for follow-up."
    />
  );
}
