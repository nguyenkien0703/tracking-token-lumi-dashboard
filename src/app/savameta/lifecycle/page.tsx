import ComingSoon from "@/components/savameta/ComingSoon";

export default function LifecyclePage() {
  return (
    <ComingSoon
      title="Lifecycle"
      description="Active (≤7d) / At-risk (8-30d) / Dormant (>30d) bucket distribution with drill-down."
    />
  );
}
